USE userinfo;
CREATE TABLE users (userid INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY, username VARCHAR(30) NOT NULL, email VARCHAR(50) NOT NULL, passhash VARCHAR(23) NOT NULL, salt VARCHAR(20) NOT NULL, verified BOOLEAN);
