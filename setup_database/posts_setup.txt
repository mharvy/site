USE maindb;
CREATE TABLE IF NOT EXISTS posts (postid INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY, userid INT UNSIGNED NOT NULL, title VARCHAR(200), date DATE NOT NULL, time TIME NOT NULL, text TEXT)
